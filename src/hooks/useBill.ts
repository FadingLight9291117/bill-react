export const useBill = () => {
    // const [bills, setBills] = useState<IBill[]>([])
    //
    // useEffect(() => {
    //     getBills().then(setBills).catch(console.dir)
    // }, [])
    // return {
    //     bills,
    // }
}